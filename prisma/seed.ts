// prisma/seed.ts
// Seed intentionally disabled in Phase-1 stabilization.
// This prevents build/type issues and stops accidental destructive seeding.
//
// If you need seed data later, we will add a safe, tenant-scoped seeder with explicit CLI command.

export { };
